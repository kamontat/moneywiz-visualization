# Affected Specs:
{{name}}

List of specifications affected by this change. ## Modified Specs | Spec | Change Type | Description
| |------|-------------|-------------|
{{#each modified_specs}}
	| [{{name}}](../../specs/{{slug}}/spec.md) |
	{{change_type}}
	|
	{{description}}
	|
{{/each}}

## New Specs | Spec | Purpose | |------|---------|
{{#each new_specs}}
	|
	{{name}}
	|
	{{purpose}}
	|
{{/each}}

## Deprecated Specs | Spec | Reason | Replacement | |------|--------|-------------|
{{#each deprecated_specs}}
	|
	{{name}}
	|
	{{reason}}
	|
	{{replacement}}
	|
{{/each}}

## No Changes

{{#each no_changes}}
	-
	{{name}}:
	{{reason}}
{{/each}}