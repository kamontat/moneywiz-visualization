# Changelog: tech-lib-structure

All notable changes to this specification.

## 1.0.0

### Added

- Initial definition of `src/lib` structure splitting `analytics` and `csv` into modules.
- Defined module boundaries for `analytics` (totals, categories, filters, etc.).
- Defined module boundaries for `csv` (parser, transformer, types).
